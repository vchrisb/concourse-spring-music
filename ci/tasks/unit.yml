---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: gradle
    tag: "jdk8"

inputs:
  - name: git-repo

run:
  user: root
  dir: git-repo
  path: sh
  args:
  - -exc
  - |
    gradle -v
    gradle test
